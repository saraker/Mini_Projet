# To-Do List Application (Python CLI)

A simple, PEP 8 compliant command-line interface (CLI) application to manage daily tasks.

## Features
- Load and Save tasks automatically via JSON.
- Add new tasks.
- View tasks in a formatted table.
- Mark tasks as completed.

## Installation
1. Clone the repository:
   ```bash
   git clone <YOUR_REPO_URL>